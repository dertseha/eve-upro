/**
 * A result notifier is for making results known to the outside world
 */
function ResultNotifier()
{
   /**
    * Notifies request results to listeners specified by given names
    * 
    * @param notifyNames an array of strings
    */
   this.notifyRequestResults = function(notifyNames)
   {

   };
}

module.exports = ResultNotifier;
