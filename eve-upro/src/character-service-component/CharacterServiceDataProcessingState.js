/**
 * A service data processing state handles character specific requests/states
 */
function CharacterServiceDataProcessingState()
{
   /**
    * Character state handler
    */
   this.onCharacterSessionAdded = function(sessionId)
   {

   };

   /**
    * Broadcast handler
    */
   this.processBroadcast = function(header, body)
   {

   };
}

module.exports = CharacterServiceDataProcessingState;
